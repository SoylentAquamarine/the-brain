# requirements.txt — Python dependencies for The Brain
#
# Install with:  pip install -r requirements.txt
#
# All AI SDKs are optional — the system gracefully skips providers whose
# SDK is not installed or whose API key is missing.

# ── Core ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0       # Load .env files automatically
pyyaml>=6.0                # Parse config/models.yaml

# ── AI Provider SDKs ──────────────────────────────────────────────────────
anthropic>=0.40.0          # Claude (orchestrator + worker)
openai>=1.50.0             # GPT-4o-mini and family
google-genai>=1.0.0        # Gemini 2.0 Flash (replaces deprecated google-generativeai)
groq>=0.11.0               # Llama 3 on Groq LPUs (free tier)
cohere>=5.0.0              # Command-R (free trial tier)
mistralai>=1.0.0           # Mistral Small / Codestral (free tier)
cerebras-cloud-sdk>=1.0.0  # Llama on Cerebras wafer silicon (free tier, fastest)

# ── Dev / testing (not needed in production) ──────────────────────────────
pytest>=8.0.0
pytest-mock>=3.14.0
